# SWAT Tactical 3D 🎮

Тактический 3D-шутер от третьего лица с волнами зомби, системой магазина и прогрессией персонажа.

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Node](https://img.shields.io/badge/node-%3E%3D18.0.0-green.svg)
![Three.js](https://img.shields.io/badge/three.js-r160-orange.svg)

## Особенности

- **3D-графика** на базе Three.js с тенями и освещением
- **Вид от третьего/первого лица** (переключение клавишей V)
- **Два вида оружия**: пистолет и дробовик
- **Система брони** с тремя уровнями защиты
- **Магазин** для покупки оружия, брони и патронов
- **Физика персонажа** с прыжками и коллизиями
- **Волны зомби** с AI преследования
- **Система хедшотов** с эффектами взрыва головы
- **Награды** за убийства и хедшоты

## Управление

| Клавиша | Действие |
|---------|----------|
| W/A/S/D | Движение |
| ЛКМ | Стрельба |
| 1 | Выбрать пистолет |
| 2 | Выбрать дробовик |
| B | Открыть/закрыть магазин |
| V | Переключить вид камеры |
| Space | Прыжок |
| ESC | Закрыть магазин |

## Установка

```bash
# Клонирование репозитория
git clone https://github.com/username/swat-tactical-3d.git
cd swat-tactical-3d

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка для продакшена
npm run build
```

## Структура проекта

```
swat-tactical-3d/
├── public/
│   └── index.html          # HTML-шаблон
├── src/
│   ├── config/
│   │   └── constants.js    # Конфигурация игры
│   ├── core/
│   │   ├── Engine.js       # Игровой движок
│   │   ├── Scene.js        # Сцена Three.js
│   │   └── Lighting.js     # Освещение
│   ├── entities/
│   │   ├── Player.js       # Игрок
│   │   ├── Enemy.js        # Враг (зомби)
│   │   ├── Weapon.js       # Оружие
│   │   └── GoreEffect.js   # Эффекты крови
│   ├── systems/
│   │   ├── CollisionSystem.js  # Коллизии
│   │   ├── RaycastSystem.js    # Райкастинг
│   │   ├── SpawnerSystem.js    # Спавн врагов
│   │   └── InputManager.js     # Управление
│   ├── state/
│   │   ├── EventBus.js     # Система событий
│   │   └── GameState.js    # Состояние игры
│   ├── ui/
│   │   ├── HUD.js          # Интерфейс
│   │   ├── Shop.js         # Магазин
│   │   ├── Inventory.js    # Инвентарь
│   │   └── UIManager.js    # Менеджер UI
│   ├── styles/
│   │   └── main.css        # Стили
│   └── main.js             # Точка входа
├── tests/
│   ├── EventBus.test.js
│   ├── GameState.test.js
│   ├── CollisionSystem.test.js
│   └── constants.test.js
├── .github/
│   └── workflows/
│       └── ci.yml          # CI/CD
├── package.json
├── vite.config.js
├── vitest.config.js
└── README.md
```

## Архитектура

### Модульная структура

Проект построен на принципах модульной архитектуры:

- **Core** - базовые компоненты движка (рендеринг, сцена, освещение)
- **Entities** - игровые сущности (игрок, враги, оружие)
- **Systems** - игровые системы (коллизии, стрельба, спавн)
- **State** - управление состоянием (EventBus, GameState)
- **UI** - пользовательский интерфейс

### Система событий

Компоненты общаются через EventBus:

```javascript
import { eventBus } from './state/EventBus.js';
import { EVENTS } from './config/constants.js';

// Подписка на событие
eventBus.on(EVENTS.ENEMY_KILLED, (data) => {
    console.log(`Убит враг! +${data.reward} монет`);
});

// Отправка события
eventBus.emit(EVENTS.ENEMY_KILLED, { reward: 50 });
```

### Управление состоянием

Централизованное состояние через GameState:

```javascript
import { gameState } from './state/GameState.js';

// Получение значения
const health = gameState.get('player.health');

// Обновление значения
gameState.setState('player.health', 75);

// Методы для частых операций
gameState.addCoins(50);
gameState.damage(25);
gameState.switchWeapon('shotgun');
```

## Тестирование

```bash
# Запуск тестов
npm test

# Запуск тестов в режиме наблюдения
npm run test:watch

# Запуск тестов с покрытием
npm run test:coverage
```

## Скрипты

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера |
| `npm run build` | Сборка проекта |
| `npm run preview` | Предпросмотр сборки |
| `npm test` | Запуск тестов |
| `npm run test:watch` | Тесты в режиме наблюдения |
| `npm run test:coverage` | Тесты с покрытием |

## Конфигурация

Все игровые параметры настраиваются в `src/config/constants.js`:

```javascript
export const PLAYER = {
    SPEED: 8,           // Скорость движения
    HEIGHT: 2.5,        // Высота персонажа
    JUMP_FORCE: 10,     // Сила прыжка
    COLLISION_RADIUS: 0.6
};

export const ZOMBIE = {
    SPEED: 2,           // Скорость зомби
    HEALTH: 100,        // Здоровье
    DAMAGE: 10,         // Урон
    SPAWN_INTERVAL: 3000 // Интервал спавна (мс)
};
```

## CI/CD

Проект использует GitHub Actions для:

- Автоматического тестирования на Node.js 18.x и 20.x
- Сборки проекта
- Деплоя на GitHub Pages (при пуше в main)

## Технологии

- [Three.js](https://threejs.org/) - 3D графика
- [Vite](https://vitejs.dev/) - сборщик
- [Vitest](https://vitest.dev/) - тестирование
- ES6 Modules

## Лицензия

MIT License
